import { defineConfig } from "@trigger.dev/sdk/v3";

export default defineConfig({
  project: "proj_tqudfdxtekiqpyjqbwvy",   // ← replace with your project ref from Trigger.dev dashboard
  dirs: ["./src/trigger"],
  maxDuration: 300,
});
